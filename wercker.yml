box: ubuntu:14.04
build:
  steps:
    - install-packages:
      packages: curl
    - script:
      name: Base Packages
      code: ./install_packages
    - script:
      name: Ruby
      code: |
        ./install_rvm
        ruby -v
        gem -v
    - script:
      name: Nginx
      code: |
        ./install_nginx
        nginx -v
